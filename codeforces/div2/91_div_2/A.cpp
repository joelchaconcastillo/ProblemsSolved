#include <bits/stdc++.h>
using namespace std;

int main()
{
  int T,n;
  while(T--)
  {
    cin >> n;
    int tmp;
    pair<int, int> pi, pj, pk;
    cin >> tmp;
    pi = pj = pk = make_pair(0, tmp);
    for(int a = 1; a < n; a++)
    { 
	cin >> tmp;
    }
  }
  return 0;
}
